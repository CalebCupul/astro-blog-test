---
const { currentPage, totalPages } = Astro.props;
---

<nav aria-label="Page navigation">
  {currentPage > 1 && <a class="bg-blue-600 text-slate-100 rounded-md px-3 py-2 mr-4 w-10" href={`/blog/${currentPage - 1}`}>Previous</a>}
  {currentPage < totalPages && <a class="bg-blue-600 text-slate-100 rounded-md px-3 py-2" href={`/blog/${currentPage + 1}`}>Next</a>}
</nav>      